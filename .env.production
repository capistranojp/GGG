# .env.production
# This file is used when you run `npm run build` or `npm run deploy`.
# It tells the React app where to find your hosted proxy in production.
#
# Replace the URL below with your actual Render (or other host) proxy URL.
# If you're deploying mock-data only (no proxy), leave this blank or delete this file.

REACT_APP_PROXY_URL=https://ggg-we1m.onrender.com/api/igdb
